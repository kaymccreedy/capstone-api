@orders.map do |order|
  {
    id: order.id,
    product_id: order.product_id,
    photo_id: order.photo_id,
    quantity: order.quantity,
    subtotal: order.subtotal,
    tax: order.tax,
    total: order.total,
  }
end
